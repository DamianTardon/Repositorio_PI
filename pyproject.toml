[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mi_proyecto"  # O el nombre que quieras
version = "0.1.0"

# Esta sección le dice a Python que el código fuente está dentro de "src"
[tool.setuptools.packages.find]
where = ["src"]